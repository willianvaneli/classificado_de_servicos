class Anuncio {
    constructor(anunciante_id, categoria,valor,descricao) {
    this.anunciante_id = anunciante_id;
    this.categoria = categoria;
    this.valor = valor;
    this.descricao = descricao;
    }};

module.exports = Anuncio;
